export default function SplashHeader() {
    return (
        <header className="flex items-center justify-center mb-6">
            <h1 className="text-4xl sm:text-5xl font-extrabold tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-blue-500 to-red-500 drop-shadow-md">Compass</h1>
        </header>
    )
}